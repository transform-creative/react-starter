/******************************
 * Helper.tsx
 * Shared data-shape utilities — joins between supabase rows, aggregations,
 * derived fields that don't belong in any single Fetch/Insert/Update/Delete
 * function. Avoid putting raw supabase calls here — those go in their layer.
 */

export {};
