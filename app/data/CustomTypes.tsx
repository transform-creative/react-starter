/*************************************************************************
 * Project-specific composite types. Add types that combine Supabase rows,
 * UI state, or domain concepts here. Pure DB row types should live in
 * `supabase.ts` (generated via `supabase gen types`).
 */

export {};
